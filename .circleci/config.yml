version: 2
jobs:
  build:
    docker:
      - image: debian:stretch
    steps:
      - run:
         name: setup
         command: apt-get update && apt-get install -y gfortran cmake gcc
      - checkout
      - run:
          name: Greeting
          command: echo Hello, world.
      - run:
          name: Print the Current Time
          command: date
